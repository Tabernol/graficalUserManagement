CREATE TABLE krasnopolskyi_kzvo.users (
                                          id INT PRIMARY KEY,
                                          name VARCHAR(80) NOT NULL,
                                          password VARCHAR(255) NOT NULL
);